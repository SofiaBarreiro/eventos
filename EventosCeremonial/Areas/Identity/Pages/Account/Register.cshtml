@page
@model RegisterModel
@{
    ViewData["Title"] = "Regístrese";
}

<h1>@ViewData["Title"]</h1>

<div class="row">
    <div class="col-md-12">
        <form asp-route-returnUrl="@Model.ReturnUrl" method="post">
@*
            @if (@Model.ModelState != null)
            {
                int cantidadErrores = 0;


                foreach (var modelState in ViewData.ModelState.Values)
                {
                    var errors = modelState.Errors;
                    if (errors.Any())
                    {
                        cantidadErrores = cantidadErrores + 1;
                    }
                }
               <p class="text-danger"> Cantidad de errores:  @cantidadErrores.ToString()</p>


            }*@
            <div class="col-xl-12" style="margin-top:40px;">
        @foreach (var modeleState in Model.ModelState.Values)
         {
            foreach (var error in modeleState.Errors)
             {
               <p class="text-danger"> @error.ErrorMessage</p>

             }
          }
     </div>

                        <h4 >Usuarios externos</h4>
            <hr />
            <div class="form-group col-md-6">
                <label asp-for="Input.Email">Correo electrónico</label>
                <input asp-for="Input.Email" class="form-control" />
                <span asp-validation-for="Input.Email" class="text-danger"></span>
            </div>
            <div class="form-group col-md-6">
                <label asp-for="Input.Password">Contraseña</label>
                <input asp-for="Input.Password" class="form-control" data-val-length="El máximo de caracteres es 10 y el mínimo 6." data-val-length-max="10" data-val-length-min="6"/>
                <span asp-validation-for="Input.Password" class="text-danger"></span>
            </div>
            <div class="form-group col-md-6">
                <label asp-for="Input.ConfirmPassword">Confirmar contraseña</label>
                <input asp-for="Input.ConfirmPassword" class="form-control" data-val-length="El máximo de caracteres es 10 y el mínimo 6." data-val-length-max="10" data-val-length-min="6"/>
                <span asp-validation-for="Input.ConfirmPassword" class="text-danger"></span>
            </div>
            <button type="submit" class="btn btn-primary">Regístrese</button>
            <a type="button" href="/Identity/Account/Login" class="btn btn-danger">Volver</a>

        </form>
            <br />
            <div class="col">

            <div class="alert alert-danger col-md-4" role="alert">
                <p class="text-danger">La contraseña debe contener como mínimo 6 caracteres y como máximo 10.</p>
            </div>
           </div>

    </div>
    @*<div class="col-md-6 col-md-offset-6">
        <section>
        <h4>Use otro servicio para ingresar.</h4>
        <hr />
        @{
        if ((Model.ExternalLogins?.Count ?? 0) == 0)
        {
        <div>
        <p>
        No hay servicio externo configurado
        </p>
        </div>
        }
        else
        {
        <form id="external-account" asp-page="./ExternalLogin" asp-route-returnUrl="@Model.ReturnUrl" method="post" class="form-horizontal">
        <div>
        <p>
        @foreach (var provider in Model.ExternalLogins)
        {
        <button type="submit" class="btn btn-primary" name="provider" value="@provider.Name" title="Log in using your @provider.DisplayName account">Active directory</button>
        }
        </p>
        </div>
        </form>
        }
        }
        </section>
        </div>*@
</div>

@section Scripts {
<partial name="_ValidationScriptsPartial" />
}
